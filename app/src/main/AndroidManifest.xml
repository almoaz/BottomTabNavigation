<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.approxsoft.materialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialDesign">
        <activity android:name=".activity.BottomTabNavigationSimpleActivity"></activity>
        <activity android:name=".activity.BottomTabNavigationBadgeBlinkActivity" />
        <activity android:name=".activity.BottomTabNavigationAnimationActivity" />
        <activity android:name=".activity.BottomTabNavigationIconActivity" />
        <activity android:name=".activity.BottomTabNavigationNightActivity" />
        <activity android:name=".activity.BottomTabNavigationColorShiftActivity" />
        <activity android:name=".activity.BottomTabNavigationDarkActivity" />
        <activity android:name=".activity.BottomTabNavigationLightActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>